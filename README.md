# Slides for PyCon 2023

## Requirements:
* `asdf`

## Install deps

```
asdf install
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Start

```
make && open index.htm
```
